# Railway deployment wrapper for the upstream Hermes Agent image.
# Avoids Railway parsing/building the full upstream multi-stage Dockerfile.
FROM nousresearch/hermes-agent:latest

ENV API_SERVER_HOST=0.0.0.0
EXPOSE 8642

CMD ["gateway", "run"]
